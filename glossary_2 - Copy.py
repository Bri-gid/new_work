glossary={
    'list':'list is a data structure in python that is mutable,ordered sequence of elements.',
    'tuple':'tuple is a data structure in python that is immutable,ordered sequence of elements.',
    'append':'append() in python adds a single element to an existing list.',
    'sorted':'sorted() funtion returns a sorted list from the iterable object.',
    'del':'del keyword in python is primarily used to delete objects in python.',
    'dictionary':'A dictionary is an unordered and mutable Python container that stores mappigsof unique keys to values.',
    }
for keyword,meaning in glossary.items():
    print(keyword.title())
    print(meaning)
